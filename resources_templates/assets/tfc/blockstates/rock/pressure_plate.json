{
	"forge_marker": 1,
	"defaults": {
		"model": "stone_pressure_plate_up",
		"uvlock": true
	},
	"variants": {
		"powered": {
			"false": {
				"model": "stone_pressure_plate_up"
			},
			"true": {
				"model": "stone_pressure_plate_down"
			}
		},
		"stonetype": {<%for (i=0; i < types.size; i++) { %>
    		  "${types[i]}": {
							"textures": {
								"texture": "tfc:blocks/rock/$texture_variant/${types[i]}"
							}}<% if(i < types.size - 1) { %>, <%}} %>
			}<%for (i=0; i < types.size; i++) { %>,
      "inventory=${types[i]}": [
          { "model": "pressure_plate_up",
            "textures": { "texture": "tfc:blocks/rock/$texture_variant/${types[i]}" }
          }
						]<%
					} %>
	}
}
